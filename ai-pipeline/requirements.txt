# PetPal AI管线 - Python依赖
# pip install -r requirements.txt

# 图像处理核心
Pillow>=10.0.0
numpy>=1.24.0

# 去背景（可选，不可用时回退到阈值法）
rembg>=2.0.50

# HTTP请求（调用AI API）
requests>=2.31.0

# 命令行工具
argparse  # 标准库，无需额外安装
